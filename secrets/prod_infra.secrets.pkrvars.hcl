/*

    Used to store secrets for use in VSCode Tasks (locally triggered runs)
    This file is allowed in .gitignore, but no others are. Don't commit secrets to Git.

    The start of this file name should match the environment you're building against

    Examples:
    -  prod_infra.secrets.pkrvars.hcl
    -  prod_vdi.secrets.pkrvars.hcl

    Remember, the vSphere_username value needs to be fully qualified

    Examples:
    -  admalangley@take2.abc.corp
    -  svcpacker@lab5.t010.net
    -  administrator@vpshere.local (no...)

*/

vSphere_username = "admapahlazani@take2.abc.corp"
vSphere_password = "Dibbulallu123~"
